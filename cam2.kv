#:import XCamera kivy_garden.xcamera.XCamera
FloatLayout:
    orientation: 'vertical'
    XCamera:
        id:xcamera

    Button:
        text: 'Change Camera'
        size_hint: .2, .1
        on_release: app.change_cam(*args)